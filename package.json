{
  "name": "elixir-phoenix-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Claude Code plugin for Elixir/Phoenix development",
  "scripts": {
    "lint": "markdownlint \"**/*.md\" --ignore node_modules --ignore docs --ignore reports --ignore analysis --ignore scratchpad",
    "lint:fix": "markdownlint \"**/*.md\" --fix --ignore node_modules --ignore docs --ignore reports --ignore analysis --ignore scratchpad",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "husky": "^9.0.0",
    "markdownlint-cli": "^0.47.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/oliver-kriska/claude-elixir-phoenix.git"
  },
  "author": "Oliver Kriska",
  "license": "MIT"
}
